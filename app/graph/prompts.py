"""Agent 提示词集中管理。

把提示词单独放一个文件：好维护、好对比不同版本（这也是 Langfuse 里
可以做 prompt 版本管理的基础）。
"""

PLANNER_PROMPT = """你是调研任务规划 Agent。请把调研主题拆分为 2~4 个相互独立、可并行搜索的子任务。

调研主题：{topic}

要求：
1. 每个子任务聚焦一个可搜索的子主题，topic 字段直接给出适合搜索的关键词。
2. intent 说明要搜集什么信息（市场数据、竞品、背景、趋势等）。
3. 只输出 JSON 数组，不要任何解释文字。

示例输出：
[{{"id": "1", "topic": "子主题1关键词", "intent": "搜集市场数据"}},
 {{"id": "2", "topic": "子主题2关键词", "intent": "搜集竞品信息"}}]
"""

EXTRACTOR_PROMPT = """你是一个信息抽取 Agent。从下面网页内容中提取与「市场调研」相关的关键事实，
重点是数据类信息（市场规模、增长率、市场份额、营收、预测值等）。

要求：
1. 只提取内容中明确出现的信息，不要推断或补充内容里没有的数据。
2. value 必须是一个纯数字（如 264.4、32），**严禁填入任何文字或单位**；
   单位单独填 unit 字段（如 "亿美元"、"%"）。
   示例：正文"市场规模达 264.4 亿美元" → value=264.4, unit="亿美元"；
        正文"同比增长 32%" → value=32, unit="%"。
3. quote 摘录支持该事实的原文句子（不超过 80 字）。
4. 每个事实给 confidence（0~1），来源模糊或数字不清晰的给低分。

网页标题：{title}
网页链接：{url}

网页内容：
{content}
"""
