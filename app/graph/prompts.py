"""Agent 提示词集中管理。

把提示词单独放一个文件：好维护、好对比不同版本（这也是 Langfuse 里
可以做 prompt 版本管理的基础）。
"""

PLANNER_PROMPT = """你是调研任务规划 Agent。请把调研主题拆分为 2~4 个相互独立、可并行搜索的子任务。

调研主题：{topic}

要求：
1. 每个子任务聚焦一个可搜索的子主题，topic 字段直接给出适合搜索的关键词。
2. intent 说明要搜集什么信息（市场数据、竞品、背景、趋势等）。
3. 只输出 JSON 数组，不要任何解释文字。

示例输出：
[{{"id": "1", "topic": "子主题1关键词", "intent": "搜集市场数据"}},
 {{"id": "2", "topic": "子主题2关键词", "intent": "搜集竞品信息"}}]
"""

EXTRACTOR_PROMPT = """你是一个信息抽取 Agent。从下面网页内容中提取与「市场调研」相关的关键事实，
重点是数据类信息（市场规模、增长率、市场份额、营收、预测值等）。

要求：
1. 只提取内容中明确出现的信息，不要推断或补充内容里没有的数据。
2. value 必须是一个纯数字（如 264.4、32），**严禁填入任何文字或单位**；
   单位单独填 unit 字段（如 "亿美元"、"%"）。
   示例：正文"市场规模达 264.4 亿美元" → value=264.4, unit="亿美元"；
        正文"同比增长 32%" → value=32, unit="%"。
3. quote 摘录支持该事实的原文句子（不超过 80 字）。
4. 每个事实给 confidence（0~1），来源模糊或数字不清晰的给低分。

网页标题：{title}
网页链接：{url}

网页内容：
{content}
"""

ANALYZER_PROMPT = """你是数据分析 Agent。下面是从多个网页来源抽取的事实清单（已按维度分组，
每条含数值、单位、时间、置信度、来源可信度、原文摘录）。

任务：交叉验证 → 去噪 → 找共识，输出 3~8 个最关键的数据点。

要求：
1. 同维度数值接近的多条事实 → 合并成一个数据点：value 取可信来源的共识值，
   source_count 填印证来源数，sources 填这些来源的 URL。
2. 同维度数值明显矛盾 → 不强行合并；采用可信来源的主流口径，并在 conflict
   字段简要说明分歧（如"不同来源口径不一：x亿元 vs y亿元"）。
   无冲突时 conflict 留空字符串，严禁写"无"这类占位词。
3. 单位不同（亿元 vs 亿美元 vs 万亿元）不能直接比较 → 换算成同一单位再合并；
   无法可靠换算的，在 conflict 里说明口径差异，不硬凑。
4. 丢弃明显噪声：可信度低、与调研主题无关、无价值重复的事实。
5. dimension 用统一简洁的表述（"中国AI市场规模"和"AI市场规模"若指同一概念就合并）。
6. value 必须是纯数字，严禁含文字或单位；单位单独填 unit。
7. quote 摘录最可信来源的原文（不超过 80 字）。
8. confidence 综合考虑来源可信度与事实置信度（0~1）。

事实清单：
{facts}
"""

JUDGE_PROMPT = """你是评测 Agent。请给下面这份多 Agent 调研系统的产出打分（每项 1~5 分，5 最好）。

调研主题：{topic}

系统产出的关键数据点（含原文摘录、来源 URL、冲突标注）：
{key_points}

请逐项评估以下 4 个指标，每个都要给 1~5 的整数分和一句具体理由（引用数据点佐证）：
1. relevance（相关性）：数据点是否贴合调研主题、信息是否有用
2. groundedness（可溯源性）：数据点是否有原文摘录与来源 URL 支撑、能否防幻觉
3. completeness（覆盖度）：主题的关键维度（市场规模 / 增长率 / 竞争格局 / 应用 / 趋势）覆盖是否全面
4. conflict_handling（冲突处理）：多来源数值冲突时是否标注分歧而非硬合并

评分从严不从宽，宁可给低分也不虚高，保证有区分度。
"""
