# 多 Agent 自动化调研分析系统 — 项目计划书

> 目标：输入一个调研主题 → 多 Agent 集群自动完成「检索 → 抽取 → 分析 → 总结」→ 输出结构化 Markdown 调研报告。
> 定位：Agent 实习生简历项目，同时是可演示、可评测的完整工程系统。

---

## 1. 技术决策摘要（已定案）

| 模块 | 选型 | 成本 |
|---|---|---|
| 语言 / 环境 | Python 3.11 + uv（或 venv） | 0 |
| 编排 | LangGraph（锁定稳定版本） | 0 |
| 大模型 | 阿里云百炼 Qwen，按角色分级（见 §2） | 约 ¥10~30 / 全程 |
| 模型接入 | OpenAI 兼容模式（`langchain-openai` → DashScope 兼容端点） | 0 |
| 搜索 | Tavily 免费档（1000 credits / 月） | 0 |
| 网页抓取 | Playwright（JS 页面）+ trafilatura（静态页） | 0 |
| 结构化输出 | Pydantic v2 + function calling / JSON 模式 | 0 |
| 去重 | 轻量规则去重（URL / 标题 / 域名），**不做 embedding 语义去重** | 0 |
| 数据 / 分析 | pandas + DuckDB | 0 |
| 状态 / 存储 | SQLite（LangGraph checkpoint + 任务持久化） | 0 |
| 观测 | Langfuse（Docker 自托管） | 0 |
| MCP | FastMCP，整个系统暴露为 MCP Server | 0 |
| 演示 | FastAPI + Streamlit（可选） | 0 |
| 评测 | 自研 eval harness，Qwen-max 打分 | 0 |

**总支出：0 元软件成本 + 模型约 ¥10~30。**

---

## 2. 大模型分级设计

统一走百炼 OpenAI 兼容端点：`https://dashscope.aliyuncs.com/compatible-mode/v1`，API Key 放环境变量 `DASHSCOPE_API_KEY`。**模型按角色分级，高量低价值调用用便宜的，低量高价值调用用贵的。**

| 角色 | 模型 | 用途 | 原因 |
|---|---|---|---|
| 规划 Agent | `qwen-plus` | 拆分调研子任务 | 中等推理够用 |
| 搜索 Agent | `qwen-flash` | 生成搜索 query | 最高频，用最便宜 |
| 抽取 Agent | `qwen-flash` | 结构化抽取（function calling / JSON 模式） | 高频 + 强类型输出 |
| 分析 Agent | `qwen-plus` | 数据分析 / 交叉验证 | 数字分析要稳 |
| 报告 Agent | `qwen-max` | 最终报告撰写 | 输出质量影响 Demo |
| 评测 Agent | `qwen-max` | 报告质量打分 | 打分要用最强模型 |

> 模型接入层抽象为「按用途选模型」的配置，换模型只改配置不改代码。简历可写：模型无关的接入层 + 分级成本控制。

---

## 3. Agent 角色与职责

| Agent | 输入 | 输出 |
|---|---|---|
| **planner（任务规划）** | 调研主题 | 子任务列表 `[{id, 子主题, 搜索意图}]` |
| **searcher（搜索）** | 子任务 | 去重后的来源列表 `[{url, title, snippet, 域名可信度}]` |
| **extractor（信息抽取）** | 来源内容 | 结构化事实 `[{维度, 数值, 来源, 时间}]`（强类型 schema） |
| **analyzer（数据分析）** | 结构化事实 | 关键数据点、趋势、冲突标注、结论 |
| **writer（报告撰写）** | 分析结果 | 结构化 Markdown 调研报告 |
| **judge（评测，离线）** | 报告 + 主题 | 质量分（完整性 / 准确性 / 结构 / 可溯源性） |

---

## 4. 系统架构与数据流

```
输入调研主题
   │
   ▼
[planner] 拆分 N 个子任务
   │
   ├── Send 并行扇出 ──► [searcher × N]  ──► 轻量去重（URL/标题/域名）
   │                          │
   │                          ▼
   │                   [extractor] 逐来源结构化抽取
   │                          │
   │                          ▼
   └──────► 汇总 ──► [analyzer] 交叉验证 / 冲突标注
                          │
                          ▼
                    [writer] 生成 Markdown 报告
```

**关键图特性（LangGraph）：**

- **并行扇出**：planner 产出 N 个子任务后，用 LangGraph `Send` API 并行执行搜索，而非串行。
- **条件边（replan）**：若某子任务搜索结果 < 阈值（如 2 条有效来源），条件边回跳 planner 重新规划该子任务。
- **重试策略**：搜索 / 抓取节点挂重试（指数退避）。
- **Checkpoint**：LangGraph checkpointer 用 SQLite 持久化，任务中断可从断点续跑。

---

## 5. 轻量去重策略（已确认：不做 embedding）

不使用 `text-embedding-v3` 余弦相似度（非必需）。采用 4 层轻量规则，随取随用、可解释：

1. **URL 去重**：去重 URL 指纹（去协议/`www.`/尾部 `/`/query 排序）。
2. **标题归一化去重**：去标点、空白、大小写后精确匹配。
3. **域名可信度**：白名单高可信（`gov`、机构官网、权威媒体）／黑名单过滤（营销、聚合站）；低可信域名降权标注。
4. **标题近似相似度（可选）**：`difflib.SequenceMatcher` 设阈值（如 >0.85）判重复，成本极低。

> 若后续评测发现重复率过高，再按需引入 embedding 语义去重，作为独立模块替换第 4 层，不影响整体架构。

---

## 6. 结构化抽取设计

- **schema**：Pydantic v2 定义，如 `Fact(source_id, dimension, value, unit, time, confidence)`、`Source(url, title, credibility)`、`ReportSection(...)`。
- **实现**：抽取 Agent 用 function calling / JSON 模式强制返回强类型，禁止自由发挥。
- **冲突处理**：同一维度多来源数值不一致 → analyzer 用 judge 交叉验证，报告中标注「多来源不一致」，不臆断。

---

## 7. MCP 设计（FastMCP）

整个系统暴露为 MCP Server，任何 MCP 客户端（Claude Desktop / Claude Code）可直接调用：

| 工具 | 说明 |
|---|---|
| `research_topic(topic, depth)` | 提交调研主题 → 触发整个 Agent 集群 → 返回 `task_id` |
| `get_research_status(task_id)` | 查询任务进度（规划中 / 搜索中 / 抽取中 / 分析中 / 完成 / 失败） |
| `get_research_result(task_id)` | 拉取完成的 Markdown 报告 |
| `search_web(query)` | 原子搜索工具（供外部复用） |

**简历卖点**：调研系统本身是 MCP Server，可被任何 MCP 客户端调用 —— 可演示、可测试。

---

## 8. 观测设计（Langfuse 自托管）

- `docker-compose.yml` 一键拉起 Langfuse（开源免费）。
- LangGraph 节点（规划 / 搜索 / 抽取 / 分析 / 报告）逐节点埋 span，可视化 token 消耗、耗时、成本。
- 评测打分结果写入 Langfuse，可配截图做简历演示。

---

## 9. 评测方案

- **评测集**：10~20 个覆盖不同领域的调研主题（技术 / 行业 / 市场 / 政策）。
- **打分**：judge（Qwen-max）按 4 维评分：完整性、准确性、结构、可溯源性。
- **量化指标**（简历数据）：
  - 报告平均质量分（0~10）
  - 抽取准确率（人工抽查 10 条事实比对）
  - 单报告成本（¥）
  - 平均耗时（分钟）
- **人工抽查**：每个主题抽检关键数据点，校验来源可溯。

---

## 10. 目录结构（规划）

```
subAgent/
├── app/
│   ├── main.py                # FastAPI / MCP 入口
│   ├── graph/
│   │   ├── state.py           # ResearchState（TypedDict）
│   │   ├── builder.py         # LangGraph 图组装
│   │   ├── nodes/             # planner / searcher / extractor / analyzer / writer
│   │   └── prompts.py
│   ├── models/
│   │   ├── llm.py             # 百炼 OpenAI 兼容接入 + 分级配置
│   │   └── schemas.py         # Pydantic 结构化 schema
│   ├── search/
│   │   ├── tavily.py          # Tavily 客户端
│   │   └── dedup.py           # 轻量去重（§5）
│   ├── scraping/
│   │   ├── playwright_client.py
│   │   └── trafilatura_client.py
│   ├── mcp/
│   │   └── server.py          # FastMCP 工具（§7）
│   ├── observability/
│   │   └── trace.py           # Langfuse 初始化与埋点
│   ├── eval/
│   │   └── judge.py           # 评测 harness
│   └── storage/
│       └── db.py              # SQLite 任务/报告持久化
├── config/
│   ├── .env.example
│   └── settings.py
├── tests/
├── docker-compose.yml         # Langfuse
├── requirements.txt
└── README.md
```

---

## 11. 实施阶段与里程碑

| 阶段 | 内容 | 验收标准 |
|---|---|---|
| **P0 环境骨架** | 项目结构、百炼接入、LangGraph 最小图（hello 级串联） | 一条命令跑通最小链路 |
| **P1 搜索 + 抓取 + 去重** | Tavily 接入、Playwright/trafilatura 抓取、轻量去重 | 单主题能搜到 ≥N 条去重来源 |
| **P2 结构化抽取** | Pydantic schema + function calling | 返回强类型事实，无自由文本 |
| **P3 分析 + 报告** | analyzer 交叉验证 + writer 生成 Markdown | 输出完整结构化报告 |
| **P4 MCP 封装** | FastMCP 工具化 | Claude Desktop 能直接调起调研 |
| **P5 观测 + 评测** | Langfuse 埋点、eval harness、指标跑通 | 出 4 项量化指标 |
| **P6 打磨** | 重试/容错/成本控制/README/截图 | 可对外演示 |

---

## 12. 风险与注意

- **Tavily 免费额度**：1000 credits / 月，评测期注意用量；预留 Bing F0（1000 次/月）作备用源，接口做「可替换搜索源」抽象。
- **LangGraph 版本**：版本迭代快、教程易过时，**锁版本**、以官方文档为准。
- **反爬/限流**：Playwright 抓取注意目标站限流，加延时与重试。
- **模型幻觉**：报告必须带来源引用，analyzer 负责冲突标注，杜绝无来源结论。

---

## 13. 简历亮点摘要（可直接引用）

> **Multi-Agent 自动化调研分析系统**：基于 LangGraph 的规划/搜索/抽取/分析/报告五角色 Agent 集群；用 `Send` API 实现并行搜索扇出，SQLite checkpoint 支持断点续跑，搜索不足自动 replan；Tavily + Playwright/trafilatura 检索抓取，Pydantic 强类型抽取 + 交叉验证处理多来源冲突；整体封装为 MCP Server 可被任意 MCP 客户端调用；Langfuse 全链路 trace 与成本可视化；评测集 20 主题，报告质量分 X、抽取准确率 Y%、单报告成本 ¥Z、平均耗时 M 分钟。
